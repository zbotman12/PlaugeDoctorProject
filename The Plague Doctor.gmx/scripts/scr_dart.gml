
// Destroys on collision
/*
if (place_meeting(x + sign(vx), y + sign(vy), obj_wall) || place_meeting(x, y, obj_Soldier)) {
    with (obj_dart) {
        instance_change(obj_ammo, false);
    }
} */

